import React from "react";
import { BrandMark } from "./BrandMark";

/** Replaces Payload's default nav icon with the site's brand badge. */
export function Icon() {
  return <BrandMark size={30} />;
}
