# دليل استخدام لوحة إدارة المحتوى (Payload CMS)

هذا الدليل لفريق تحرير المحتوى: كيفية الدخول، تعديل الصفحات والخدمات
والمشاريع، رفع الصور، والنشر بأمان دون كسر شيء في الموقع العام.

## الدخول إلى اللوحة

الرابط: `/admin` على نطاق الموقع (مثال: `https://your-domain.com/admin`).

لكل مستخدم دور (Role) يحدد ما يمكنه رؤيته وتعديله:

| الدور | الصلاحيات |
| --- | --- |
| **super-admin** | كل شيء، بما في ذلك حذف السجلات وإدارة المستخدمين |
| **content-manager** | إنشاء/تعديل المحتوى (الحلول، القطاعات، المشاريع، المقالات) لكن لا يحذف نهائيًا |
| **sales** | الطلبات (Leads) وسجل المتابعات فقط — لا يرى المحتوى التحريري |
| **analyst** | التحليلات وإعداداتها فقط — قراءة |
| **viewer** | قراءة فقط لكل المحتوى المنشور والمسودات |

إذا احتجت دورًا جديدًا أو حسابًا لموظف، اطلب من super-admin إنشاءه من
**Users** في اللوحة.

## هيكل المحتوى

### Collections (سجلات متعددة)

- **الحلول (Solutions)** — كل خدمة (إدارة مرافق، صيانة، نظافة...) بصفحتها
  الخاصة على `/solutions/[slug]`.
- **القطاعات (Sectors)** — القطاعات التي تخدمها الشركة (تجاري، صحي، تعليمي...).
- **المشاريع ودراسات الحالة (Projects)** — لا تُنشر أي دراسة حالة على الموقع
  العام إلا إذا كانت **منشورة (Published)** و**verificationStatus = verified**
  معًا. راجع `CONTENT-VERIFICATION.md` لفهم هذا القيد وسببه.
- **مركز المعرفة (Insights)** — مقالات ونصائح.
- **الأسئلة الشائعة، شهادات العملاء، شعارات العملاء، شهادات الاعتماد،
  أعضاء الفريق، الشواغر الوظيفية، الصفحات العامة، الوسائط (Media)**.

### Globals (سجل واحد لكل نوع)

- **إعدادات الشركة، بيانات التواصل، الصفحة الرئيسية، صفحة من نحن، صفحة
  الجودة والسلامة، الهيدر والفوتر، إعدادات SEO، إعدادات التحليلات،
  إعدادات الإشعارات**.
- صفحتا **من نحن** و**الجودة والسلامة** تُعرضان مباشرة (بدون خطوة نشر
  منفصلة) — أي تعديل تحفظه هنا يظهر على الموقع العام فورًا.

## لغة واجهة اللوحة نفسها

بالإضافة إلى ترجمة المحتوى، واجهة اللوحة نفسها (الأزرار، القوائم، رسائل
النظام) تدعم العربية والإنجليزية. لتبديلها: **الحساب (Account)** من القائمة
العلوية → **اللغة (Language)**. هذا منفصل تمامًا عن قائمة **Locale** الخاصة
بترجمة المحتوى الموضحة أدناه.

## الترجمة (عربي/إنجليزي)

معظم الحقول قابلة للترجمة (تظهر أيقونة اللغة بجانب الحقل). بدّل بين اللغتين
من قائمة **Locale** أعلى يمين الشاشة. القاعدة:

- اللغة العربية هي اللغة الافتراضية (fallback) — إذا لم تُترجم حقلًا للإنجليزية
  بعد، سيظهر النص العربي تلقائيًا في النسخة الإنجليزية من الموقع حتى تُكمل
  الترجمة. هذا مقصود (لا فراغات في الموقع) لكن راجع كل صفحة قبل الإعلان
  عنها للجمهور الإنجليزي.

## المسودات والنشر (Drafts & Publish)

كل سجل في Solutions وSectors وProjects وInsights وPages له حالتان:

- **Draft (مسودة)** — يظهر في اللوحة فقط، غير مرئي على الموقع العام.
- **Published (منشور)** — مرئي على الموقع العام.

عند التعديل على سجل منشور، التعديلات تُحفظ كمسودة تلقائيًا (autosave) دون
التأثير على النسخة المنشورة، حتى تضغط **Publish** يدويًا. هذا يسمح بمراجعة
التغييرات قبل ظهورها للجمهور.

## رفع الصور والوسائط

من **Media** أو مباشرة من داخل أي حقل صورة:

1. ارفع الملف.
2. **يجب** ملء حقل **النص البديل (Alt)** بالعربية والإنجليزية — مطلوب
   لإمكانية الوصول (WCAG) ومحركات البحث.
3. حدد **التصنيف (Category)** إن وُجد لتسهيل البحث لاحقًا.
4. إذا كانت الصورة من عميل أو تحتاج إذن استخدام، فعّل **usageApproved** فقط
   بعد التأكد من الحصول على الإذن الكتابي.

⚠️ لا ترفع صورًا لعملاء أو مواقع مشاريع دون تأكيد إذن النشر من العميل أولًا.

## إضافة حل (خدمة) جديد

1. **الحلول → إنشاء جديد**.
2. املأ العنوان، الملخص، `heroOutcome`، `problem`، `methodology`، `quality`
   بالعربية أولًا، ثم بدّل للإنجليزية وأكمل الترجمة.
3. أضف عناصر **النطاق (scope)** كقائمة نقطية.
4. اربط **القطاعات (sectors)** ذات الصلة.
5. أضف صورة رئيسية (heroImage) بنص بديل.
6. اضغط **Publish** بعد المراجعة.

## إضافة مشروع / دراسة حالة

⚠️ **قاعدة صارمة:** لا تُنشئ دراسة حالة إلا ببيانات حقيقية موثّقة من العميل
أو فريق التشغيل. لا تخترع أرقامًا أو نسب تحسّن.

1. **المشاريع ودراسات الحالة → إنشاء جديد**.
2. حدد **وضع السرية (confidentialityMode)**:
   - "اسم العميل ظاهر" — فقط إذا وافق العميل كتابيًا على ذكر اسمه.
   - "سري" — استخدم وصفًا عامًا مثل "جهة في القطاع الصحي" بدل الاسم الفعلي.
3. أي مؤشر أداء (KPI) يجب أن يكون له **sourceNote** يوضح مصدر الرقم (هذا
   الحقل داخلي فقط ولا يُنشر).
4. ارفع **إثبات موافقة العميل (approvalEvidence)** — بريد أو خطاب موافقة —
   في الحقل المخصص بالشريط الجانبي (مرئي فقط لـ super-admin وcontent-manager).
5. اضبط **verificationStatus** إلى "verified" فقط بعد التأكد الفعلي من
   البيانات، ثم **Publish**. لن تظهر الدراسة على الموقع العام قبل تحقق
   الشرطين معًا.

## نصائح عامة

- استخدم **معاينة (Preview)** إن توفرت قبل النشر النهائي.
- لا تحذف سجلًا نهائيًا (Delete) إلا إذا كنت متأكدًا — هذه الصلاحية مقصورة
  على super-admin لغالبية المحتوى لهذا السبب بالضبط.
- عند الشك في صحة معلومة (رقم هاتف، شهادة، اسم شركة)، اتركها بحالة "غير
  مؤكدة" (`unverified`/`pending`) بدل نشرها — راجع `CONTENT-VERIFICATION.md`.
