# دليل لوحة المبيعات وإدارة الطلبات (Leads)

هذا الدليل لفريق المبيعات: كيفية متابعة الطلبات الواردة من نموذج "اطلب
عرضًا" ونموذج التوظيف، وتحديث حالتها، وقراءة لوحة النظرة السريعة.

## من أين تأتي الطلبات؟

كل طلب من:

- **نموذج طلب عرض/معاينة** على صفحة `/contact` (4 خطوات: نوع العميل،
  الخدمة، بيانات التواصل، المراجعة والإرسال)
- **نموذج الاهتمام الوظيفي** على صفحة `/careers`

يُحفظ فورًا في قاعدة البيانات كسجل في **الطلبات (Leads)** — لا يُفقد أي طلب
حتى لو تعطّل إشعار البريد/الواتساب.

## لوحة النظرة السريعة (Dashboard)

تظهر فور الدخول إلى `/admin`، وتُخصَّص حسب دورك:

- **بطاقات الأرقام**: طلبات اليوم / هذا الأسبوع / هذا الشهر / طلبات جديدة
  غير معالجة.
- **متابعات مستحقة اليوم أو متأخرة**: أي طلب له تاريخ متابعة (followUpAt)
  حان موعده ولم يُغلق بعد — راجعها أول كل يوم.
- **تقسيم الطلبات**: حسب الخدمة، القطاع، المدينة، والمصدر (UTM) — لمعرفة
  من أين يأتي أغلب الطلب الفعلي.
- **آخر الطلبات**: أحدث 5 طلبات مع رابط مباشر لكل سجل.

الأرقام هنا **حقيقية دائمًا** — لا تُعرض أي أرقام افتراضية أو تقديرية. إذا
رأيت "0" فهذا يعني عدم وجود بيانات فعلًا، وليس خطأ.

## حالة الطلب (Status) — دورة الحياة

```
جديد → تم التواصل → مؤهل → زيارة مجدولة → موعد محدد → عرض مرسل → فاز / خسر
                                                                  └─ Spam
```

- **جديد**: الحالة الافتراضية عند وصول أي طلب. راجعه خلال ساعات العمل.
- **Spam**: للطلبات الوهمية الواضحة (لا تحذفها — غيّر الحالة فقط، حتى يبقى
  السجل للمراجعة إن لزم).
- **خسر**: عند التحديد، يظهر حقل **سبب الخسارة (lossReason)** — املأه دائمًا،
  فهذا يساعد على تحسين العروض لاحقًا.

## متابعة طلب

1. افتح السجل من **الطلبات**.
2. حدّث **الحالة** حسب آخر تواصل.
3. اضبط **تاريخ المتابعة القادمة (followUpAt)** — سيظهر في قسم "متابعات
   مستحقة" باللوحة عند حلول موعده.
4. عيّن الطلب لموظف مبيعات محدد عبر **assignedTo** إن كان الفريق أكثر من
   شخص.
5. سجل ملاحظة في **سجل أنشطة الطلبات (LeadActivities)** — هذا سجل دائم لا
   يُحذف (append-only)، يوثّق كل تغيير حالة ومحادثة.

## تصدير البيانات

من قائمة **الطلبات**، استخدم تصدير Payload القياسي (CSV) لتنزيل الطلبات
المفلترة — مفيد لتقارير أسبوعية أو رفعها لنظام CRM خارجي لاحقًا.

## حالة الربط بالأنظمة الخارجية

يظهر هذا القسم فقط لـ super-admin في أسفل لوحة النظرة السريعة، ويوضّح بصدق
ما هو مربوط فعليًا وما لا يزال يحتاج إعدادًا:

- **إشعارات الطلبات (Webhook)** — عند عدم الربط، الطلبات تُحفظ في قاعدة
  البيانات وتظهر هنا فورًا، لكن لن يصلك تنبيه فوري (Slack/بريد). اربط
  `NOTIFY_WEBHOOK_URL` لتفعيله (راجع `DEPLOYMENT.md`).
- **تخزين الوسائط** — إن ظهر "تخزين محلي"، فالمرفقات لن تبقى بعد أي تحديث
  للموقع على معظم منصات الاستضافة. يجب ربط تخزين سحابي قبل الإطلاق الفعلي.
- **البريد الإلكتروني والتحليلات** — نفس المبدأ: لا شيء يعمل فعليًا حتى
  تُضاف بيانات الاعتماد الحقيقية.

## أسئلة شائعة

**سؤال: طلب وصل بدون اسم خدمة محددة، ماذا أفعل؟**
تحقق من حقل `serviceSlug` (النص الخام كما أُرسل من النموذج) — أحيانًا لا
يمكن ربطه تلقائيًا بسجل خدمة إذا كانت الخدمة غير موجودة بعد في قائمة الحلول.

**سؤال: هل يمكن حذف طلب نهائيًا؟**
فقط super-admin يملك هذه الصلاحية، ويُفضّل تجنبها — استخدم حالة "Spam" بدلًا
من الحذف للحفاظ على سجل كامل.
